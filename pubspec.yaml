name: uuid_plus
version: 0.1.0
description: >
  RFC4122 (v1, v4, v5, v6, v7, v8) UUID Generator and Parser for Dart
repository: https://github.com/input-output-hk/uuid_plus

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  crypto: ^3.0.6
  meta: ^1.16.0
  fixnum: ^1.1.1
  
dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.2